int sensorValue = analogRead(A0); //leer una entrada de voltaje por ese pin (OJO NO DEBE SER >5V)

mysqli_fetch_array //Obtiene una fila de resultados como un array asociativo, numérico, o ambos

$result = mysqli_query($con,"SELECT * FROM ESPtable2");//obtenemos la tabla en un array

PHP isset(): Comprobar si una variable está definida. Rápido y sencillo post sobre la función PHP isset(), dicha función comprueba si una variable está definida o no en el script de PHP que se está ejecutando.
